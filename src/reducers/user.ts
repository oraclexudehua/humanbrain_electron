import {
  SETTINGS_SAVED,
  SETTINGS_PAGE_UNLOADED,
  ASYNC_START,
} from '../constants/actionTypes';

const initState = {
  menu: [],
};
export default (state = initState, action: any) => {
  switch (action.type) {

    default:
      return state;
  }
};
